import React from "react";
import { View, Text } from "react-native";

export default function Posts() {
    return (
        <View style={{backgroundColor:'#afaf', flex: 1, justifyContent: "center", alignItems:"center"}}>
        <Text style={{fontSize:30}}>POSTS</Text>
    </View>
)
}